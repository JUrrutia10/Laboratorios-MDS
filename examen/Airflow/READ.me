Toda la parte de mlops esta en esta carpeta de airflow, para poder ejecutarlo se debe agregar un archivo a la carpeta con nombre 'variables.json'

aqui es un dict y una de las varibles debe ser {"api_token": "eltoken"}

hay cuatro archivos dockerfile, 3 son las imagenes de los programas, airflow, mlflow, gradio y por ultimo el compose

en la carpeta dags esta el codigo ex.py, donde estan todas las funciones que utiliza el dag